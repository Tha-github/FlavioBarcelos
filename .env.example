# Obrigatória no deploy: origem HTTPS do site, sem barra no fim (ex.: https://www.exemplo.com.br).
# Usada para gerar URLs absolutas nos dados estruturados (JSON-LD). Configurar no painel da hospedagem.
SITE_ORIGIN=
